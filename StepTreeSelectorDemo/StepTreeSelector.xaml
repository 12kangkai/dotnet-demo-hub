<UserControl x:Class="StepTreeSelectorDemo.StepTreeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StepTreeSelectorDemo"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="StepNodeButtonStyle" TargetType="{x:Type ui:Button}">
            <Setter Property="MinWidth" Value="136" />
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ui:Button}">
                        <Border
                            x:Name="Root"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Padding="14,10"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#F5FAFF" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#84CAFF" />
                                <Setter Property="Foreground" Value="#175CD3" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#EFF8FF" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#2E90FA" />
                                <Setter Property="Foreground" Value="#1849A9" />
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#175CD3" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#175CD3" />
                                <Setter Property="Foreground" Value="White" />
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.56" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NodeIconBadgeStyle" TargetType="Border">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Background" Value="#EEF4FF" />
            <Setter Property="CornerRadius" Value="7" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#FFFFFF" />
                    <Setter Property="Opacity" Value="0.22" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border
        Padding="{Binding Padding, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
        Background="#F9FAFB"
        BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
        BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
        CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}">

        <DockPanel LastChildFill="True">

            <ui:Button
                DockPanel.Dock="Top"
                Margin="0,0,0,12"
                HorizontalAlignment="Left"
                Appearance="Primary"
                Command="{Binding MaintainCommand, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
                Content="{Binding EmptyButtonText, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
                Icon="{ui:SymbolIcon AddCircle32}"
                Visibility="{Binding IsEmpty, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}, Converter={StaticResource BoolToVisibilityConverter}}" />

            <ScrollViewer
                x:Name="PART_ScrollViewer"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">

                <ItemsControl
                    x:Name="PART_LevelsItemsControl"
                    ItemsSource="{Binding VisibleLevels, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <DockPanel LastChildFill="False" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="DockPanel.Dock" Value="Top" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:StepTreeLevel}">
                            <Border
                                Margin="0,0,0,12"
                                Padding="14"
                                Background="White"
                                BorderBrush="#EAECF0"
                                BorderThickness="1"
                                CornerRadius="8">
                                <DockPanel LastChildFill="True">

                                    <TextBlock
                                        DockPanel.Dock="Left"
                                        Width="{Binding LevelHeaderWidth, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
                                        Margin="0,8,10,0"
                                        VerticalAlignment="Top"
                                        FontSize="13"
                                        FontWeight="SemiBold"
                                        Foreground="#475467"
                                        Text="{Binding Title}" />

                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type local:StepTreeItem}">
                                                <ui:Button
                                                    Command="{Binding SelectCommand, RelativeSource={RelativeSource AncestorType=local:StepTreeSelector}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource StepNodeButtonStyle}">
                                                    <StackPanel Orientation="Horizontal"
                                                                HorizontalAlignment="Center">
                                                        <TextBlock
                                                            VerticalAlignment="Center"
                                                            FontSize="13"
                                                            FontWeight="SemiBold"
                                                            Text="{Binding DisplayName}" />
                                                        <ui:SymbolIcon
                                                            Width="14"
                                                            Height="14"
                                                            Margin="12,0,0,0"
                                                            VerticalAlignment="Center"
                                                            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ui:Button}}"
                                                            Symbol="ArrowRight16"
                                                            Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                    </StackPanel>
                                                </ui:Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </DockPanel>
    </Border>
</UserControl>
