<Window x:Class="StepTreeSelectorDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StepTreeSelectorDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="StepButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Opacity" Value="0.7" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="18"
            FontWeight="SemiBold"
            Margin="0,0,0,16"
            Text="报警原因逐级选择 Demo" />

        <Border
            Grid.Row="1"
            Padding="12"
            BorderThickness="1"
            BorderBrush="#999"
            CornerRadius="6">

            <DockPanel LastChildFill="False">

                <Button
                    DockPanel.Dock="Top"
                    Content="+ 未维护，点击维护"
                    Command="{Binding MaintainCommand}"
                    Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}" />

                <ItemsControl
                    DockPanel.Dock="Top"
                    ItemsSource="{Binding VisibleLevels}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <DockPanel LastChildFill="False" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="DockPanel.Dock" Value="Top" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:StepTreeLevel}">
                            <DockPanel Margin="0,6" LastChildFill="True">

                                <TextBlock
                                    DockPanel.Dock="Left"
                                    Width="64"
                                    Margin="0,12,8,0"
                                    VerticalAlignment="Top"
                                    Text="{Binding Title}" />

                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type local:TreeModel}">
                                            <Button
                                                Style="{StaticResource StepButtonStyle}"
                                                Content="{Binding DisplayName}"
                                                Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </DockPanel>
        </Border>

        <TextBlock
            Grid.Row="2"
            Margin="0,16,0,0"
            FontSize="16"
            Text="{Binding SelectedReason.DisplayName, StringFormat=最终选择：{0}, TargetNullValue=最终选择：未选择叶子节点}" />
    </Grid>
</Window>
